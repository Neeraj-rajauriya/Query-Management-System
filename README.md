# Query-Management-System
The Query Management System is a full-stack app that enables users to submit complaints and admins to manage them. Built with React, Node.js, and MongoDB, it features role-based access, allowing only admins to view all complaints while users manage their own. JWT handles secure authentication.
